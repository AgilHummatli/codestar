/*
 * Task 41: Check if Character Swap is Possible
 * Input: Two characters (A, B)
 * Output: "Possible" (always possible using ASCII values)
 */

#include <iostream>
using namespace std;

int main() {
    char a, b;
    cin >> a >> b;
    
    cout << "Possible";
    return 0;
}
